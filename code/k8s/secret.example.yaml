# Template only — copy to `secret.yaml`, fill in real values, and DO NOT commit.
# In production these should come from your secrets manager (Vault, AWS Secrets
# Manager, GCP Secret Manager, etc.) via External Secrets Operator or similar,
# not from a checked-in YAML.
apiVersion: v1
kind: Secret
metadata:
  name: ege-rag-secrets
  namespace: ege
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://USER:PASS@postgres:5432/ege_generator"
  KAFKA_BOOTSTRAP_SERVERS: "kafka-broker.kafka:9092"
  PLATFORM_AUTH_TOKEN: "REPLACE_ME_HS256_JWT"
  OPENROUTER_API_KEY: "REPLACE_ME"
